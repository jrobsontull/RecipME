{
  "name": "recipme",
  "version": "1.0.0",
  "description": "",
  "main": "/backend/index.js",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm run build",
    "install-frontent": "cd frontent && npm install",
    "heroku-postbuild": "npm run install-frontend && cd backend && npm run build",
    "start": "cd backend && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.2.2"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
